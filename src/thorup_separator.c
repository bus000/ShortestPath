#include "thorup_dist.h"
#include "planarity.h"

/*linked_list_t thorup_separator(planar_digraph_t const *graph)*/
linked_list_t thorup_separator(planar_digraph_t const *graph)
{
    linked_list_t list;
    /* Find a breath first spanning tree T. */

    /* Triangulate graph. */

    /* Select any nontree edge (v1, w1) and compute T-cycle. Compute cost of
     * each side. */

    return list;
}
